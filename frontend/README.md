# Task Manager Frontend (Vite + React)

## Prereqs
- Node.js 18+ and npm

## Setup
```bash
cd frontend
npm install
npm run dev
```

## Backend
The app uses `http://13.205.146.153` as the API base URL (see `src/services/api.js`).

